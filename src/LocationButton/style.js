import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  locationButton: {
    backgroundColor: '#FFDDFF',
    width: 80,
    padding: 25,
    borderRadius: 5
  },
});